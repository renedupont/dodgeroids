<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.games.keepitup"
    android:versionCode="2"
    android:versionName="1.1" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/game_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="de.games.keepitup.KeepItUpActivity" android:label="@string/game_name" android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
   		<activity android:name="de.games.keepitup.ExceptionActivity" android:label="@string/game_name"></activity>
    </application>

</manifest>
